interface:
  display_name: "Doc Init"
  short_description: "Establish or refresh documentation coverage"
  default_prompt: "Use $doc-init to establish or refresh documentation coverage and its global lifecycle preset."

policy:
  allow_implicit_invocation: true
